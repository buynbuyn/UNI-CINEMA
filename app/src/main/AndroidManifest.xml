<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UNI_CINEMA"
        tools:targetApi="31">
        <activity
            android:name=".ui.phongchieu.DeskActivity"
            android:exported="false"
            android:theme="@style/Theme.UNI_CINEMA.NoActionBar"/>
        <activity
            android:name=".ui.thanhtoan.ThanhToanResult"
            android:exported="true"
            android:launchMode="singleTop"> <!-- Giữ singleTop cho an toàn -->

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="payment-result"
                    android:scheme="uni_cinema" />
            </intent-filter>

        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />


                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="payment-result"
                    android:scheme="uni_cinema" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.phim.MovieDetailsActivity"
            android:exported="true"
            android:theme="@style/Theme.UNI_CINEMA.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.UNI_CINEMA.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.RegisterActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.UNI_CINEMA.NoActionBar" />
        <activity
            android:name=".login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.UNI_CINEMA.NoActionBar" />
        <activity android:name=".login.QuenMKActivity"
            android:exported="true"
            android:theme="@style/Theme.UNI_CINEMA.NoActionBar"/>
    </application>

</manifest>